<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="at.usmile.gaitmodule.GaitAuthenticationTest">

    <TextView
        android:id="@+id/gaitAuthenticationTestTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="This is simple application to call GaitAuthentication serivces" />

    <Button
        android:id="@+id/stopService"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/startService"
        android:layout_centerVertical="true"
        android:onClick="stopServicePressed"
        android:text="Stop" />

    <Button
        android:id="@+id/startService"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/stopService"
        android:layout_alignLeft="@+id/gaitAuthenticationTestTextView"
        android:layout_marginBottom="31dp"
        android:onClick="startServicePressed"
        android:text="Start" />

</RelativeLayout>
